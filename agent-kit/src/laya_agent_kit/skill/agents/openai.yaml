interface:
  display_name: "Laya Local Decisions"
  short_description: "Proactive local passage ranking and browser/frontend choices"
  default_prompt: "Use $laya to rank supplied passages or compare browser/frontend candidates, reuse the session, batch related questions, and verify the evidence."
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: "mcp"
      value: "laya"
      description: "Local offline passage ranking and typed judgments"
